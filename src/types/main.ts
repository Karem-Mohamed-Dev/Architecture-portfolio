export type EdCard = {
  education: string;
  sub?: string;
  date?: string;
};
export type ExCard = {
  experience: string;
  sub?: string;
  date?: string;
};

export type head = { letter: string; title: string };


export type skill = {
  name: string,
  imgType: string,
  skillPercint: string,
  color: string
}